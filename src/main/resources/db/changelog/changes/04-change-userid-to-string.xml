<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="4-fixed-v3" author="kai">
        <sql dbms="postgresql">
            ALTER TABLE card_info DROP CONSTRAINT IF EXISTS fk_card_user;
            ALTER TABLE card_info ALTER COLUMN user_id TYPE VARCHAR(255) USING (user_id::VARCHAR(255));
            ALTER TABLE users DROP CONSTRAINT IF EXISTS users_pkey CASCADE;
            ALTER TABLE users ALTER COLUMN id DROP IDENTITY IF EXISTS;
            ALTER TABLE users ALTER COLUMN id TYPE VARCHAR(255) USING (id::VARCHAR(255));
            ALTER TABLE users ADD PRIMARY KEY (id);
            ALTER TABLE card_info ADD CONSTRAINT fk_card_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;
            ALTER TABLE users DROP COLUMN IF EXISTS keycloak_id;
        </sql>
        
        <sql dbms="!postgresql">
            -- Fallback for non-postgres
            ALTER TABLE card_info DROP CONSTRAINT fk_card_user;
            ALTER TABLE card_info MODIFY user_id VARCHAR(255);
            ALTER TABLE users DROP PRIMARY KEY;
            ALTER TABLE users MODIFY id VARCHAR(255);
            ALTER TABLE users ADD PRIMARY KEY (id);
            ALTER TABLE card_info ADD CONSTRAINT fk_card_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;
            ALTER TABLE users DROP COLUMN keycloak_id;
        </sql>
    </changeSet>
</databaseChangeLog>
